# space, path

# space: (x, y, w, h)
# path: from assets
# layers at the beginning in ea. level are behind
# objects are always behind everything

LAYERS = [[[(0, 0, 10, 10), "bg"]], None, None, [[(0, 2, 1, 1), "vines"]]]
